# The following command can be used to install a pytorch version compaible with python 3.10.0 on Windows. A cpu only version can be install from the link below:

pip install torch==2.1.2 torchvision==0.16.2 torchaudio==2.1.2 --index-url https://download.pytorch.org/whl/cu121

https://pytorch.org/get-started/previous-versions/